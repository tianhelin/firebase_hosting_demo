# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to GCE while main branch updated
'on':
  push:
    branches:
      - main
jobs:
  deploy_to_gcp:
    runs-on: ubuntu-latest
    steps:
      - uses: 'actions/checkout@v2'
      - id: 'auth'
        uses: 'google-github-actions/auth@v0'
        with:
          workload_identity_provider: 'projects/805483161008/locations/global/workloadIdentityPools/my-pool'
          service_account: 'deploy-service-account@huichengtw-83263.iam.gserviceaccount.com'
     - uses: google-github-actions/ssh-compute@v0.1.5
       with:
        instance_name: instance-1
        zone: asia-east1-b
        ssh_private_key: '${{ secrets.GCP_SSH_PRIVATE_KEY }}'
        command: |
          cd /var/www
          rm -rf firebase_hosting_demo
          git clone git@github.com:tianhelin/firebase_hosting_demo.git